#pragma config(Sensor, S1,     luce,           sensorLightActive)
#pragma config(Sensor, S4,     distanza,       sensorSONAR)
//*!!Code automatically generated by 'ROBOTC' configuration wizard               !!*//

#include "wait.h"
task main()
{

	//waitRotations(0, 10, 30);
	//while(true){writeDebugStreamLine("%d", SensorValue(luce));wait(1);}
	motor[motorA] = 50;
	waitLight(luce, 50, true);
	motor[motorA] = 10;
	waitLight(luce, 50, false);

}
